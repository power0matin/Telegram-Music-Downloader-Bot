name: Link official project page once
on:
  push:
    paths: [".github/workflows/link-official-project-page-once.yml"]
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Add first-party project reference
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('README.md')
          text = p.read_text(encoding='utf-8')
          url = 'https://matinshahabadi.ir/projects/telegram-music-downloader-bot/'
          if url not in text:
              marker = '<!-- repo-badges:end -->'
              line = '\n\n> **Official project page:** [matinshahabadi.ir/projects/telegram-music-downloader-bot/](https://matinshahabadi.ir/projects/telegram-music-downloader-bot/)'
              text = text.replace(marker, marker + line, 1) if marker in text else line.lstrip() + '\n\n' + text
              p.write_text(text, encoding='utf-8')
          PY
      - name: Commit and remove one-shot workflow
        run: |
          git config user.name "Matin Shahabadi"
          git config user.email "116592301+power0matin@users.noreply.github.com"
          git rm -f .github/workflows/link-official-project-page-once.yml
          git add README.md
          git diff --cached --quiet && exit 0
          git commit -m "docs: link official Telegram Music Downloader project page"
          git push