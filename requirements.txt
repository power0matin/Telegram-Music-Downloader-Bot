# Core Telegram Bot API
pyTelegramBotAPI>=4.14.0,<5.0.0

# HTTP requests for metadata extraction
requests>=2.32.3,<3.0.0

# Environment variable management
python-dotenv>=1.0.0,<2.0.0

# Spotify downloader
# 4.5.2 is the first hotfix release after the 4.5 Spotify/yt-dlp changes.
# Keep this within the 4.5 CLI contract used by utils/downloader.py.
spotdl>=4.5.2,<4.6.0

# Optional: For better async support (if needed)
# aiofiles>=23.0.0,<24.0.0
